name: github
display_name: GitHub REST
description: Full GitHub REST v3 API — repos, issues, PRs, actions, search, gists.
category: developer
spec_url: https://raw.githubusercontent.com/github/rest-api-description/main/descriptions/api.github.com/api.github.com.json
spec_format: json
tier: official
homepage: https://docs.github.com/rest
notes: ~1000 endpoints. Auth via GITHUB_TOKEN (a fine-grained or classic PAT).
