# {{ bin_name }}

MCP server for **{{ spec.display_name or spec.name }}**. Generated by DuckTap.

## Install

```bash
pip install -e .
```

## Add to Claude Desktop

`~/Library/Application Support/Claude/claude_desktop_config.json` (macOS) or
`%APPDATA%\Claude\claude_desktop_config.json` (Windows):

```json
{
  "mcpServers": {
    "{{ spec.name }}": {
      "command": "{{ bin_name }}",
      "env": {
        {% for s in spec.auth_schemes %}"{{ s.env_var }}": "${{ '{' }}{{ s.env_var }}{{ '}' }}"{% if not loop.last %},{% endif %}
        {% endfor %}
      }
    }
  }
}
```

## Tools exposed ({{ tools|length }})
{% for t in tools %}
- **`{{ t.name }}`** — {{ t.description }}
{% endfor %}
