# {{ cli_bin }}

Agent-native CLI for **{{ spec.display_name or spec.name }}**, generated by [DuckTap](https://github.com/zanni098/DuckTap).

{{ spec.description }}

## Install

```bash
pip install -e .
```

## Auth

Set environment variables before running:

{% for s in spec.auth_schemes %}- `{{ s.env_var }}` — {{ s.type }}{% if s.description %} ({{ s.description }}){% endif %}{{ "\n" }}{% endfor %}

Override the base URL with `{{ spec.name|upper|replace('-','_') }}_BASE_URL` if you self-host.

Check local configuration without making API changes:

```bash
{{ cli_bin }} doctor
```

## Usage

```bash
{{ cli_bin }} --help
```

Available commands:
{% for op in operations %}- `{{ cli_bin }} {{ op.operation_id | cmd }}` — {{ op.summary or op.description or op.method + " " + op.path }}{{ "\n" }}{% endfor %}

## Global flags

| Flag | Default | Purpose |
|------|---------|---------|
| `--json` / `--pretty` | `--json` | Emit JSON for agents or rich output for humans. |
| `--select id,name` | none | Keep only selected top-level JSON fields. |
| `--compact` | off | Keep high-gravity fields for lower-token agent output. |
| `--quiet` | off | Suppress successful output while preserving exit codes. |
| `--dry-run` | off | Show the request without calling the API. |
| `--cache` / `--no-cache` | `--cache` | Mirror GETs into `~/.ducktap/{{ spec.name }}/mirror.sqlite`. |
| `--cache-ttl` | 300 | Mirror TTL in seconds. |

## Agent mode

Useful defaults for agents:

```bash
{{ cli_bin }} --json --compact <command>
{{ cli_bin }} --dry-run <command>
{{ cli_bin }} --select id,name,status <command>
```

Exit codes: `0` success, `3` not found, `4` auth, `5` API error, `7` rate limited, `10` local configuration error.

## MCP

This CLI ships with a matching MCP server, `{{ spec.name }}-dt-mcp`, so any MCP-aware agent (Claude, Cursor, etc.) can call it directly.

---

Generated by DuckTap v{{ ducktap_version }}. Provenance: `{{ spec.source.discoverer }}` from `{{ spec.source.source }}`.
