# {{ cli_bin }}

Agent-native CLI for **{{ spec.display_name or spec.name }}**, generated by [DuckTap](https://github.com/zanni098/DuckTap).

{{ spec.description }}

## Install

```bash
pip install -e .
```

## Auth

Set environment variables before running:

{% for s in spec.auth_schemes %}- `{{ s.env_var }}` — {{ s.type }}{% if s.description %} ({{ s.description }}){% endif %}
{% endfor %}

Override the base URL with `{{ spec.name|upper|replace('-','_') }}_BASE_URL` if you self-host.

## Usage

```bash
{{ cli_bin }} --help
```

Available commands:
{% for op in operations %}
- `{{ cli_bin }} {{ op.operation_id | cmd }}` — {{ op.summary or op.description or op.method + " " + op.path }}{% endfor %}

## Global flags

| Flag | Default | Purpose |
|------|---------|---------|
| `--json` / `--no-json` | `--json` | Emit JSON (agent-friendly). |
| `--pretty` | off | Rich pretty-print for humans. |
| `--cache` / `--no-cache` | `--cache` | Mirror GETs into `~/.ducktap/{{ spec.name }}/mirror.sqlite`. |
| `--cache-ttl` | 300 | Mirror TTL in seconds. |

## MCP

This CLI ships with a matching MCP server, `{{ spec.name }}-dt-mcp`, so any MCP-aware agent (Claude, Cursor, etc.) can call it directly.

---

Generated by DuckTap v0.1.0. Provenance: `{{ spec.source.discoverer }}` from `{{ spec.source.source }}`.
