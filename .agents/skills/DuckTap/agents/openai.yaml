interface:
  display_name: "DuckTap"
  short_description: "Repo-specific patterns and workflows for DuckTap"
  default_prompt: "Use the DuckTap repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true